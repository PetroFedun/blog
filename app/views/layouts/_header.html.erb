<header class="navbar navbar-fixed-top navbar-inverse bg-primary">
  <div class="container">
    <%= link_to "blog", root_path, id: "logo" %>

    <nav>
        <div id="hamburger" class="navbar-toggle collapsed">
        </div>
      <ul class="nav navbar-right" id="menu">
        <li><%= link_to "Home", root_path %></li>
        <li><%= link_to "Help", static_pages_help_path %></li>
        <% if user_signed_in? %> 
          <li class="dropdown">
          <a href="#" id="account" class="dropdown-toggle">
              Account <b class="caret"></b>
            </a>
            <ul id="dropdown-menu" class="dropdown-menu">
            <li><%= link_to "Edit Account",  edit_user_registration_path, class: "btn btn-primary btn-dropdown" %></li>
            <li><%= link_to "New Article", new_article_path, class: "btn btn-primary btn-dropdown" %></li>
            <li><%= button_to "Sign out", destroy_user_session_path,data: { confirm: "Are you sure?", turbo_confirm: "Are you sure?" }, method: :delete, class: "btn btn-danger btn-dropdown" %></li>
            </ul>
          </li>
        <% else %>
          <li class="btn btn-success"><%= link_to "Sign in",  new_user_session_path %></li>
        <% end %>
      </ul>
      </ul>
    </nav>  
  </div>
</header>

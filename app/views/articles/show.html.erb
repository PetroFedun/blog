<h1 class="text-center mb-5"><%= @article.title %></h1>

<p><%= @article.body %></p>

<ul>
  <li class="btn btn-primary mb-2"><%= link_to "Edit", edit_article_path(@article) %></li><br>
  <li class="btn btn-danger"><%= link_to "Destroy", article_path(@article), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>
</ul>

<div class="d-flex justify-content-around" style="padding-top: 50px;">
  <div> 
    <h2 class="mb-3">Add a comment</h2> 
    <%= render 'comments/form' %> 
  </div>

  <div> 
    <h2 class="mb-3">Comments</h2>
    <%= render @article.comments %> 
  </div> 
</div>
